import styled from 'styled-components';

const SectionItem = styled.section`
  padding: 15px 0;
`;

const Title = styled.h2`
  font-size: 40px;
  font-weight: bold;
`;

export { SectionItem, Title };
